[{"path":"https://fraseriainlewis.github.io/tfclinical/articles/intro_to_tfp.html","id":"overview","dir":"Articles","previous_headings":"","what":"1. Overview","title":"Bayesian model building walkthrough 1","text":"vignette shows key building blocks building Bayesian model TFP. uses mixture R Python Rmarkdown cells. R used data set creation generation figures, Python model building TFP. general theme vignettes; Python used direct interaction TFP API needed, otherwise R used assumed go-language choice statisticians.","code":""},{"path":"https://fraseriainlewis.github.io/tfclinical/articles/intro_to_tfp.html","id":"example-model---formulation","dir":"Articles","previous_headings":"1. Overview","what":"1.1 Example Model - Formulation","title":"Bayesian model building walkthrough 1","text":"model implemented TPF code following logit(θ)\\mbox{logit}(\\theta) - TBC. Also include non-centered parmeterization.","code":""},{"path":"https://fraseriainlewis.github.io/tfclinical/articles/intro_to_tfp.html","id":"make-data-available-to-tfp-models","dir":"Articles","previous_headings":"","what":"2. Make data available to TFP models","title":"Bayesian model building walkthrough 1","text":"Assuming data modelled, .e. source data parameters estimates via model fitting, loaded R generated R first step make data available TFP. functionality readily provided reticulate, one watchout - Python pandas library required deal data.frames (see installation instructions - pandas can included part Rstudio’s tfprobability install script). TF TPF models written Python use TF tensors arguments, rather familiar Python structures numpy arrays. distinction important tensors strongly typed care needed, including moving back forth R via reticulate.","code":""},{"path":"https://fraseriainlewis.github.io/tfclinical/articles/intro_to_tfp.html","id":"example-data-set","dir":"Articles","previous_headings":"2. Make data available to TFP models","what":"2.1 Example data set","title":"Bayesian model building walkthrough 1","text":"R chunk creates simple dataset three cols: binary response variable (0/1 = non-responder/responder), binary treatment variable (0/1 = control/test treatment) basket ID variable. (1) basket ID currently set fixed 1, denoting one basket trial, .e. classical two arm randomized trial design. Baskets added vignettes.","code":"### Prepare model inputs set.seed(9999) # Set up data rr_k_ctrl <- c(0.60)        # control response rate for each basket rr_k_trt <- c(0.58)         # treatment response rate for each basket  K<-length(rr_k_ctrl)        # number of baskets  N_k_ctrl <- rep(250, K)     # number of control participants per basket N_k_trt <- rep(250, K)      # number of treatment participants per basket N_k <- N_k_ctrl + N_k_trt   # number of participants per basket (both arms combined) N <- sum(N_k)               # total sample size k_vec <- rep(1:K, N_k)      # N x 1 vector of basket indicators (1 to K)  z_vec<-NULL; y<-NULL; for(i in 1:K){ # for each basket repeat 0-control 1-trt according to the specifc Ns   z_vec<-c(z_vec,rep(0:1,c(N_k_ctrl[i],N_k_trt[i]))) # treatment/control indicator   y<-c(y,        c(rbinom(N_k_ctrl[i],1,rr_k_ctrl[i]), # bernoulli for control          rbinom(N_k_trt[i],1,rr_k_trt[i]))) #           for trt }  thedata<-data.frame(y,basketID=k_vec,Treatment=z_vec)  py$thedata<-r_to_py(thedata) # THE KEY LINE - makes data available to Python                               # this is converted into a pandas dataframe object in Python"},{"path":"https://fraseriainlewis.github.io/tfclinical/articles/intro_to_tfp.html","id":"setup-python-and-get-data-from-r","dir":"Articles","previous_headings":"","what":"3. Setup python and get data from R","title":"Bayesian model building walkthrough 1","text":"first load necessary libraries TFP convert dataset passed R tensors. Note tensors strongly typed explicitly define storage type.","code":"import numpy as np import pandas as pd import os import keras import tensorflow as tf import tensorflow_probability as tfp from tensorflow_probability import distributions as tfd tfb = tfp.bijectors import warnings import time import sys  ## The data.frame passed is now a panda df but for TPF this needs to be further  ## converted into tensors here we simply convert each col of the data.frame into  ## a Rank 1 tensor (i.e. a vector) y_data=tf.convert_to_tensor(thedata.iloc[:,0], dtype = tf.float32) k_vec=tf.convert_to_tensor(thedata.iloc[:,1], dtype = tf.float32) z_vec=tf.convert_to_tensor(thedata.iloc[:,2], dtype = tf.float32)  #print(f\"y={z_vec}\") #if using locally this prints out to R console"},{"path":"https://fraseriainlewis.github.io/tfclinical/articles/intro_to_tfp.html","id":"define-the-probability-model","dir":"Articles","previous_headings":"","what":"4. Define the Probability Model","title":"Bayesian model building walkthrough 1","text":"now fully define model, .e., data likelihood prior densities. use TFP function called JointDistributionSequentialAutoBatched. number alternative TFP functions defining Bayesian models. one simplest still capable defining complex models. model definition JointDistributionSequentialAutoBatched must follow strict convention explained comments. model formulation uses non-centered approach aim MCMC estimation discussed stan manual .","code":""},{"path":"https://fraseriainlewis.github.io/tfclinical/articles/intro_to_tfp.html","id":"define-the-likelihood","dir":"Articles","previous_headings":"4. Define the Probability Model","what":"4.1 Define the likelihood","title":"Bayesian model building walkthrough 1","text":"","code":"# Define LIKELIHOOD - this is defined first, as functions need defined before used # The arguments in our function are in the REVERSE ORDER that the parameters  # appear in JointDistrbutionSequential this is essential def make_observed_dist(log_odd_control_and_ratio,sigma1, mu1,sigma0,mu0):     # beta is a two element tensor, beta[0] parameter for the log odds of the control arm effect     #                               beta[1] log odds ratio of treatment to control     beta=tf.stack([mu0 + sigma0 * log_odd_control_and_ratio[0],                             mu1 + sigma1 * log_odd_control_and_ratio[1]])                    #tf.stack is used to stack tensors without creating new tensors                         # return a vector of Bernoulli probabilities, one for each patient, and these estimates      # dependent on which arm the patient was randomized to, i.e. two unique values     return(tfd.Independent(         tfd.Bernoulli(logits=beta[0]+beta[1]*z_vec), #define as logits         reinterpreted_batch_ndims = 1 # This tells TFP that log_prob here is a single value                                       # equal to sum of individual log_probs, a vector                                       # this is usual when defining a data likelihood     ))"},{"path":"https://fraseriainlewis.github.io/tfclinical/articles/intro_to_tfp.html","id":"define-the-full-model","dir":"Articles","previous_headings":"4. Define the Probability Model","what":"4.2 Define the full model","title":"Bayesian model building walkthrough 1","text":"straightforward provided ’s done sequence. Care needed understanding whether parameters scalars vectors, especially complex models feature design matrix matrix algebra. Simulating realization model simple way see structure state variable model . complex models simulating multiple values useful check structure expected.","code":"# DEFINE FULL MODEL - hyperparams hard coded      # model is y[i] = Bernoulli(p[i]) where logit(p[i]) = intercept + treatment*z[i] model = tfd.JointDistributionSequentialAutoBatched([   tfd.Normal(loc=0., scale=2.5, name=\"mu0\"),  # prior for mean of control arm log odds    tfd.HalfNormal(scale=2.5, name=\"sigma0\"),   # prior for sd of control arm log odds   tfd.Normal(loc=0., scale=2.5, name=\"mu1\"),  # prior for mean of treatment log odds ratio   tfd.HalfNormal(scale=2.5, name=\"sigma1\"),   # prior for sd of treatment log odds ratio   # now come to the standard normals resulting from non-centred parameterization   # define these as vector MVN with identity scale matrix   tfd.Normal(loc=tf.zeros(2), scale=tf.ones(2), name=\"log_odd_control_and_ratio\"),    # now finally define the likelihood as we have already defined parameters in this   make_observed_dist # the data likelihood defined as a function above ])     tf.random.set_seed(9999) # Sample one variate from this joint probability model - shows what structure the model  # produces/needs. i.e. what the state variable of the model looks like in TF mysample=model.sample(1) # 1 = one random variate print(f\"{mysample}\") print(py$mysample)"},{"path":"https://fraseriainlewis.github.io/tfclinical/articles/intro_to_tfp.html","id":"define-the-log_prog_fn","dir":"Articles","previous_headings":"4. Define the Probability Model","what":"4.3 Define the log_prog_fn","title":"Bayesian model building walkthrough 1","text":"use various MCMC samplers provided TFP need explicitly provide function returns log posterior density. straightforward simply involves wrapping around existing model method log_prob() seen .","code":"# the ordering of the arguments in this must match exactly the ordering used in # JointDistributionSequentialAutoBatched def log_prob_fn(mu0, sigma0, mu1,sigma1, log_odd_control_and_ratio):   return model.log_prob((       mu0, sigma0, mu1,sigma1, log_odd_control_and_ratio,y_data))                                                           ## last argument is the DATA (y)   # useful to see how to call this. We use the simulated values above       mylogp=log_prob_fn(mysample[0], mysample[1], mysample[2],mysample[3], mysample[4]) print(py$mylogp)"},{"path":"https://fraseriainlewis.github.io/tfclinical/articles/normal_tf_stan.html","id":"normal-regression","dir":"Articles","previous_headings":"","what":"normal regression","title":"normal_tf_stan","text":"keywords: comparison rstanarm; Normal regression; broadcasting. just code, needs work.","code":"################################################################################ # This file fits a Bayesian linear regression (Gaussian) using public data set. # # It uses three different R Bayesian libraries: # 1. rstanarm (assumed the default use case) # 2. nimble - for comparison # 3. tensorflow - for comparison # # Note: rstanarm does some manipulation (centering and prior adjustment) which # need reflect in the the other packages # Documentation is currently very thin and code messy # chains need longer to run shorter for ease of testing # # Many libraries need installed and tensorflow can be problematic # Once the libraries are installed then the whole file can be sourced # and the output is \"plot_negbin.pdf\" which is a comparison of parameter # estimates # # F. Lewis 31-OCT-2025 ################################################################################  data(mtcars)  # Rescale # note for models that do internal predictor centering then need location shift back for intecept # i.e y = a + b*(x1-mean) + c*(x2-mean) # so want a at x1=0 and x2=0 so E(y) = mean(a) + mean(b)*-mean(x1) + mean(c)*-mean(x2) etc. # this centering does not appear to be used in neg bin - as y is counts seem reasonable # observe lambda*t = a + b + c but want just lambda, i.e. Y=lambda*t so lambda = Y/t # log(lambda) = a + b + c  + log(exposure) # P(X=x) = lambda^x exp(-lambda)/x!  lambda = lambda2*t #  default_prior_test <- stan_glm(mpg ~ wt + am, data = mtcars, chains = 1)  # Estimate original model glm1 <- glm(mpg ~ wt + am,             data = mtcars, family = gaussian) # Estimate Bayesian version with stan_glm stan_glm1 <- stan_glm(mpg ~ wt + am, data = mtcars, family = gaussian(),                       prior = normal(0, 2.5),                       prior_intercept = normal(0, 5),                       seed = 12345,                       warmup = 10000,      # Number of warmup iterations per chain                       iter = 20000,        # Total iterations per chain (warmup + sampling)                       thin = 1,                       chains = 4)           # Thinning rate) res_m<-as.matrix(stan_glm1) summary(res_m[,\"(Intercept)\"]) summary(res_m[,\"wt\"])  prior_scales<-prior_summary(stan_glm1) # get the predictor adjusted scale - stating priors explicitly so no autoscaling beta_prior_scale<-prior_scales$prior$scale # THE ABOVE NEEDS FIXED - if no priors are given this will be incorrect # note - also hard coded 5 in stan below which needs fixed #  aux is always re-scaled -next line always needed sd_prior_scale<-prior_scales$prior_aux$adjusted_scale #need 1/sd_prior_scale for exp  # log(l*n) = a + b, log(l)+log(n) =  ################################################################################ ## use base rstan ## the below is from gemini # Load necessary libraries library(rstan) library(ggplot2) library(bayesplot) library(dplyr) # For data manipulation if needed, e.g., tibble  # Set Stan options for better performance and to avoid recompilation rstan_options(auto_write = TRUE) options(mc.cores = parallel::detectCores())  # --- 1. Simulate data for the Negative Binomial Regression Model --- # (Since no data is provided, we simulate a dataset that matches the description)  set.seed(12345)  # --- Define the Stan model as a string in R ---  stan_model_string <- \" data {   int<lower=1> N;                 // Number of observations   int<lower=1> M;                 //number of predictors excl intercept   array[N] real<lower=0> y;         // Response variable (counts)   vector[N] wt;           // Continuous predictor   vector[N] am;    // Binary predictor   //Hyperparameters   array[M] real<lower=0>rescaled_sd;// standard dev for predictors  }  transformed data {   vector[N] wt_centered;   real mean_wt=mean(wt);   vector[N] am_centered;   real mean_am=mean(am);   wt_centered = wt - mean_wt;  // Center in transformed data block   am_centered = am - mean_am; }  parameters {   real alpha;                     // Intercept   real beta_wt;               // Coefficient for roach1   real beta_am;            // Coefficient for treatment   real<lower=0> phi;              // Negative Binomial overdispersion parameter }  transformed parameters {   array[N] real mu;           // Log of the mean parameter   for (i in 1:N) {     // linear model     mu[i] = alpha +                 beta_wt * wt_centered[i] +                 beta_am * am_centered[i] ;   } }  model {   // --- Priors ---   // Weakly informative priors for coefficients and intercept   alpha ~ normal(0, 5.0);           // Prior for intercept   beta_wt ~ normal(0,rescaled_sd[1] );     // Prior for roach1 coefficient   beta_am ~ normal(0, rescaled_sd[2]);  // Prior for treatment coefficient   phi ~ exponential(rescaled_sd[3]); // this is actually 1/ rescaled scale    // --- Likelihood ---   // Negative Binomial likelihood, using the log-link function for the mean   y ~  normal(mu, phi); }  generated quantities {   // Can include posterior predictions or log-likelihood here if desired for model checking   real intercept_0;   intercept_0=alpha + beta_wt*-mean_wt + beta_am*-mean_am;    } \"  # --- 3. Prepare data for Stan --- # The data needs to be provided as a list for rstan::stan() stan_data <- list(   N = nrow(mtcars),   M = 3, # number of passed hyperpriors   rescaled_sd=c(beta_prior_scale,1/sd_prior_scale),# 1/ as prior uses rate   y = mtcars$mpg,   wt = mtcars$wt,   am = mtcars$am )  # --- 4. Fit the Stan model --- # Use rstan::stan() to compile and sample from the model fit <- stan(   model_code = stan_model_string,   data = stan_data,   chains = 4,         # Number of MCMC chains   warmup = 10000,      # Number of warmup iterations per chain   iter = 20000,        # Total iterations per chain (warmup + sampling)   thin = 1,           # Thinning rate   seed = 12345,          # For reproducibility   control = list(adapt_delta = 0.95, max_treedepth = 15) # Adjust for sampling issues if needed )  # --- 5. Extract main parameters and produce density plots --- res2<-extract(fit,par=c(\"alpha\",\"beta_wt\",\" beta_am\",\"phi\",\"intercept_0\"))"},{"path":"https://fraseriainlewis.github.io/tfclinical/articles/tf_gnn_example.html","id":"graphical-neural-network-model","dir":"Articles","previous_headings":"","what":"Graphical Neural Network Model","title":"tf_gnn_example","text":"requires custom python venv setup ensure correct version compatibility. See installation guide advanced. example ported original Tensorflow GNN github site Needs compatible versions (installed via pip venv hard coding versions): Python: 3.11.14 Version: 2.16.0 Version: 2.16.2 Version: 1.0.3","code":"import os os.environ[\"TF_USE_LEGACY_KERAS\"] = \"1\"  # For TF2.16+.  import matplotlib.pyplot as plt import numpy as np import tensorflow as tf import tensorflow_gnn as tfgnn  print(f'Running TF-GNN {tfgnn.__version__} with TensorFlow {tf.__version__}.')     train_path = os.path.join(os.getcwd(), 'precomputed/mutag', 'train.tfrecords') val_path = os.path.join(os.getcwd(), 'precomputed/mutag', 'val.tfrecords') #get_ipython().system('ls -l {train_path} {val_path}')  print(f\" the train path={train_path}\") #exit()   graph_tensor_spec = tfgnn.GraphTensorSpec.from_piece_specs(     context_spec=tfgnn.ContextSpec.from_field_specs(features_spec={                   'label': tf.TensorSpec(shape=(1,), dtype=tf.int32)     }),     node_sets_spec={         'atoms':             tfgnn.NodeSetSpec.from_field_specs(                 features_spec={                     tfgnn.HIDDEN_STATE:                         tf.TensorSpec((None, 7), tf.float32)                 },                 sizes_spec=tf.TensorSpec((1,), tf.int32))     },     edge_sets_spec={         'bonds':             tfgnn.EdgeSetSpec.from_field_specs(                 features_spec={                     tfgnn.HIDDEN_STATE:                         tf.TensorSpec((None, 4), tf.float32)                 },                 sizes_spec=tf.TensorSpec((1,), tf.int32),                 adjacency_spec=tfgnn.AdjacencySpec.from_incident_node_sets(                     'atoms', 'atoms'))     })   def decode_fn(record_bytes):   graph = tfgnn.parse_single_example(       graph_tensor_spec, record_bytes, validate=True)    # extract label from context and remove from input graph   context_features = graph.context.get_features_dict()   label = context_features.pop('label')   new_graph = graph.replace_features(context=context_features)    return new_graph, label   # In[7]:   train_ds = tf.data.TFRecordDataset([train_path]).map(decode_fn) val_ds = tf.data.TFRecordDataset([val_path]).map(decode_fn)   # ### Look at one example from the dataset  # In[8]:   g, y = train_ds.take(1).get_single_element()   # #### Node features #  # Node features represent the 1-hot encoding of the atom type (0=C, 1=N, 2=O, 3=F, # 4=I, 5=Cl, 6=Br).  # In[9]:   print(g.node_sets['atoms'].features[tfgnn.HIDDEN_STATE])   # #### Bond Edges #  # In this example, we consider the bonds between atoms undirected edges. To encode # them in the GraphsTuple, we store the undirected edges as pairs of directed # edges in both directions. #  # `adjacency.source` contains the source node indices, and `adjacency.target` contains the corresponding target node indices.  # In[10]:   g.edge_sets['bonds'].adjacency.source   # In[11]:   g.edge_sets['bonds'].adjacency.target   # #### Edge features #  # Edge features represent the bond type as one-hot encoding.  # In[12]:   g.edge_sets['bonds'].features[tfgnn.HIDDEN_STATE]   # ### Label # The label is binary, indicating the mutagenicity of the molecule. It's either 0 or 1.  # In[13]:   y #for k, hist in history.history.items(): #  plt.plot(hist) #  plt.title(k) #  plt.show() for k, hist in history.history.items():     plt.figure()  # Create a new figure for each metric     plt.plot(hist)     plt.title(k)     plt.xlabel('Epoch')     plt.ylabel(k)          # Save the plot. Using f-strings to name the file based on the key (e.g., loss.png)     plt.savefig(f\"precomputed/pyplot_{k}.png\")          # Optional: If you want to show it in the console while running     # plt.show()           plt.close()        # Feel free to play with the hyperparameters and the model architecture to improve the results!"},{"path":[]},{"path":"https://fraseriainlewis.github.io/tfclinical/authors.html","id":null,"dir":"","previous_headings":"","what":"Authors","title":"Authors and Citation","text":"Fraser Lewis. Author, maintainer.","code":""},{"path":"https://fraseriainlewis.github.io/tfclinical/authors.html","id":"citation","dir":"","previous_headings":"","what":"Citation","title":"Authors and Citation","text":"Lewis F (2026). tfclinical: Package (Title Case) - just aaa placeholder. R package version 0.1.0, https://fraseriainlewis.github.io/tfclinical/.","code":"@Manual{,   title = {tfclinical: What the Package Does (Title Case) - just aaa placeholder},   author = {Fraser Lewis},   year = {2026},   note = {R package version 0.1.0},   url = {https://fraseriainlewis.github.io/tfclinical/}, }"},{"path":"https://fraseriainlewis.github.io/tfclinical/index.html","id":"why-tensorflow-probability","dir":"","previous_headings":"","what":"Why Tensorflow Probability?","title":"What the Package Does (Title Case) - just aaa placeholder","text":"Tensorflow Probability (TFP) alternative rstan. two major open source numerical libraries offering similar functionality reduces risk. rstan mature library Bayesian modelling, TFP part Google’s Tensorflow framework offers computational flexibility (CPU/GPU/TPU).","code":""},{"path":"https://fraseriainlewis.github.io/tfclinical/index.html","id":"features","dir":"","previous_headings":"","what":"Features","title":"What the Package Does (Title Case) - just aaa placeholder","text":"vignettes range demonstrating basic step--step mechanics running MCMC RStudio using TFP, example use adaptive step sizes multiple chains, complete examples simulating Bayesian basket trials using TFP comparing results rstan. vignettes include comparison rstan.","code":""},{"path":"https://fraseriainlewis.github.io/tfclinical/index.html","id":"installation-overview","dir":"","previous_headings":"","what":"Installation overview","title":"What the Package Does (Title Case) - just aaa placeholder","text":"Due computationally intensive nature code vignettes show precomputed outputs, vignettes contain necessary code repeat computations. run computations tfclinical library needs installed one main dependency tfprobability. tfprobability library directly used installing (see installation instructions ensures necessary Python libraries available. one exception, also use Python pandas library included tfprobabilty install script (see ) ``extras’’ option.","code":""},{"path":"https://fraseriainlewis.github.io/tfclinical/index.html","id":"installation-details-for-linux","dir":"","previous_headings":"Installation overview","what":"Installation details for Linux","title":"What the Package Does (Title Case) - just aaa placeholder","text":"Python needs installed, pyenv can also useful tool dealing multiple Python version avoid impacting system Python installation.","code":"########################################################## # in RStudio Terminal (not console) or Bash sudo apt-get update sudo apt-get install python3-venv python3-pip python3-dev ########################################################## # in RStudio console install.packages(\"tensorflow\") library(tensorflow) install_tensorflow(extra_packages = c(\"tf_keras\", \"tensorflow\", \"tensorflow-probability\",\"pandas\")) #restart session install.packages(\"tfprobability\") library(tensorflow) library(tfprobability) d <- tfd_binomial(total_count = 7, probs = 0.3) # if this works then tensorflow us correctly installed ##########################################################"},{"path":"https://fraseriainlewis.github.io/tfclinical/index.html","id":"installation-details-for-linux-1","dir":"","previous_headings":"Installation overview","what":"Installation details for Linux","title":"What the Package Does (Title Case) - just aaa placeholder","text":"Windows key part suitable python installation also RStudio can locate . place installation applied inside RStudio Linux case.","code":""},{"path":"https://fraseriainlewis.github.io/tfclinical/index.html","id":"installation-of-separate-python-venv","dir":"","previous_headings":"Installation overview","what":"Installation of separate Python venv","title":"What the Package Does (Title Case) - just aaa placeholder","text":"Graphical Neural Network (GNN) vignette requires separate Python venv setup instructing reticulate use ","code":"# in bash python3 -m venv myenv source myenv/bin/activate # now install via pip the specific library versions needed # in RStudio console library(reticulate)"},{"path":"https://fraseriainlewis.github.io/tfclinical/reference/hello.html","id":null,"dir":"Reference","previous_headings":"","what":"Hello, World! — hello","title":"Hello, World! — hello","text":"Prints 'Hello, world!'.","code":""},{"path":"https://fraseriainlewis.github.io/tfclinical/reference/hello.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Hello, World! — hello","text":"","code":"hello()"},{"path":"https://fraseriainlewis.github.io/tfclinical/reference/hello.html","id":"ref-examples","dir":"Reference","previous_headings":"","what":"Examples","title":"Hello, World! — hello","text":"","code":"hello() #> [1] \"Hello, world again and again!\""}]
